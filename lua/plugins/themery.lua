require("themery").setup({
    themes = { "material-darker", "material-lighter" }, -- Your list of installed colorschemes.
    livePreview = true,                                 -- Apply theme while picking. Default to true.
})
